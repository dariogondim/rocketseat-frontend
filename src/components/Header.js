import React from 'react'
import { render } from 'react-dom'

export default function Header() {
    return (
        <header>
            <h1>
                ReactJS
            </h1>
        </header>
    )
}
